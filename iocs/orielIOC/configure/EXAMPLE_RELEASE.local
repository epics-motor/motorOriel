# Use the following lines if motorOriel was built inside motor
MOTOR=
-include $(MOTOR)/configure/RELEASE
# Use the following lines if motorOriel was built outside motor
#!MOTOR_ORIEL=
#!-include $(MOTOR_ORIEL)/configure/RELEASE.local
